export type Cat = {
   fact: string,
   image: string
}

export type LoginData = {
   password: string
   email: string
}

export type User = {
   avatarUrl: string
   email: string
   id: number
   isPro: boolean
   name: string
}